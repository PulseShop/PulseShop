# PulseShop automation config — copy to automation/.env (gitignored) and fill in.
# The SERVICE ROLE key bypasses RLS — keep it server-side only, never in the frontend.
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_SERVICE_KEY=eyJhbGci...service_role_key...
